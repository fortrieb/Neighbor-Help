import { IHelpState } from './state';

export interface IHelpGetters {
  /**
   * put your getter names, parameters and return values here, for example:
   * myAction(context: ActionContext<IHelpState, IState>, param: any): Promise<any>;
   */
}

export const HelpGetters: IHelpGetters = {
  /**
   * here comes the implementation of your getters, for example:
   * myGetter(state: IHelpState) {
   *   return state.myAttribute;
   * }
   */
};
